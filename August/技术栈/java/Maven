Maven


Maven是一个软件项目管理和综合工具

Maven提供了开发人员构建一个完整的生命周期框架。开发团队可以自动完成项目的基础工具建设，Maven使用标准的目录结构和默认构建生命周期。

Maven的依赖查询顺序更改为：
	在Maven本地资源库中搜索，如果没有找到进入第二步，否则退出
	在Maven中央存储库搜索，如果没有找到进入第三步，否则退出
	在java.net Maven的远程存储库搜索，若果没有找到，提示错误信息，否则退出



定制库到Maven本地资源库

使用原因
 要使用的jar不存在于Maven的中心存储库
 创建了自定义的jar包，而另一个Maven项目需要使用


 
